apiVersion: v1
kind: Service
metadata:
  name: insights-mcp-server
  namespace: insights-mcp-server
  labels:
    app.kubernetes.io/name: insights-mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
      appProtocol: HTTP
  selector:
    app.kubernetes.io/name: insights-mcp-server
