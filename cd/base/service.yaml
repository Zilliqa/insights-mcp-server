apiVersion: v1
kind: Service
metadata:
  name: validator-mcp-server
  namespace: validator-mcp-server
  labels:
    app.kubernetes.io/name: validator-mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
      appProtocol: HTTP
  selector:
    app.kubernetes.io/name: validator-mcp-server
